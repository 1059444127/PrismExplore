<UserControl x:Class="EventAggregationExplore.View.SubjectView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Button Content="L" Width="100" Height="100" HorizontalAlignment="Center" VerticalAlignment="Center" Command="{Binding LeftLabelCommand}"></Button>
        <Button Content="R" Width="100" Height="100" HorizontalAlignment="Center" VerticalAlignment="Center" Command="{Binding RightLabelCommand}" Grid.Column="1"></Button>
    </Grid>
</UserControl>